<div id="<%= dom_id post %>">
	<h3><%= post.title %></h3>
	<p>Posted by <%= link_to post.user.name, user_path(post.user) %></p>
	<p><%= pluralize(post.views, "view") %></p>
	<div><%= post.body %></div>
</div>
